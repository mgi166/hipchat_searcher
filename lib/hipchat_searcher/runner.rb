module HipchatSearcher
  class Runner
    def initialize(pattern, room, config, options)
      @pattern = pattern
      @room    = room
      @config  = config
      @options = options
    end

    def run
    end
  end
end
